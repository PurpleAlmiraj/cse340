<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
} %>


<div class="management-links">
    <a href="/inv/add-classification" class="add-classification">Add New Classification</a>
    <a href="/inv/add-inventory" class="add-inventory">Add New Inventory</a>
</div>